plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
    id 'com.google.gms.google-services'
}

android {
    namespace = "com.example.lab2_203217"
    compileSdk = 34 // Updated to SDK 34 as required by dependencies.

    ndkVersion = "23.1.7779620" // Adjust as needed based on your project.

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
        applicationId = "com.example.lab2_203217"
        minSdkVersion = 23 // Explicitly set to match project requirements.
        targetSdkVersion = 34 // Updated to match compileSdk.
        versionCode = 1 // Adjust based on your app's versioning strategy.
        versionName = "1.0" // Adjust based on your app's versioning strategy.
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation platform('com.google.firebase:firebase-bom:33.7.0') // Firebase BOM.
}
